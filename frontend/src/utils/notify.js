import toast from 'react-hot-toast';

/**
 * Toast notification utility using react-hot-toast
 * Provides UI notifications for success, error, info, and warning messages
 */

export { toast };
export default toast;
